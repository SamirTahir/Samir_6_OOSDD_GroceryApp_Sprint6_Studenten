<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
             xmlns:m="clr-namespace:Grocery.Core.Models;assembly=Grocery.Core"
             x:Class="Grocery.App.Views.CategoriesView"
             x:DataType="vm:CategoriesViewModel"
             Title="CategoriesView">

    
        <Shell.TitleView>
        <Grid>
            <Label Text="Productcategorieën" FontSize="Large" HorizontalOptions="Start" VerticalOptions="Center" />
        </Grid>
    </Shell.TitleView>

    <ScrollView>
        <Border Stroke="#C49B33" StrokeThickness="4" Padding="1" Margin="20" HorizontalOptions="Center">
        <CollectionView ItemsSource="{Binding Categories}" Margin="20" HorizontalOptions="Center"
                SelectionMode="Single"
                SelectionChangedCommand="{Binding SelectCategoryCommand}"
                SelectionChangedCommandParameter="{Binding Source={RelativeSource Self}, Path=SelectedItem}">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="5" />
            </CollectionView.ItemsLayout>
            <CollectionView.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.8*"/>
                        <ColumnDefinition Width="8*"/>
                    </Grid.ColumnDefinitions>
                    <Border Stroke="#9880E5" Background="#9880E5" Grid.Column="0" Padding="2">
                        <Label Text="#" FontAttributes="Bold"/>
                    </Border>
                    <Border Stroke="#9880E5" Background="#9880E5" Grid.Column="1" Padding="2">
                        <Label Text="Naam" HorizontalTextAlignment="Start" FontAttributes="Bold"/>
                    </Border>
                </Grid>
            </CollectionView.Header>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="m:Category">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.8*"/>
                            <ColumnDefinition Width="8*"/>
                        </Grid.ColumnDefinitions>
                        <Border Stroke="White" Background="DimGray" Grid.Column="0" Padding="2">
                            <Label Text="{Binding Id}" FontAttributes="Bold"/>
                        </Border>
                        <Border Stroke="White" Background="DimGray" Grid.Column="1" Padding="2">
                            <Label Text="{Binding Name}" FontAttributes="Bold"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Border>
    </ScrollView>
</ContentPage>